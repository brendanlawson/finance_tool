name: finance_tool
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Selects SQLite3MultipleCiphers (SQLite3MC) as the native SQLite build used
# by package:sqlite3 / drift. This is what makes PRAGMA key based database
# encryption available at runtime (see lib/core/database/database.dart).
# SQLCipher itself (source: sqlcipher) and the old sqlcipher_flutter_libs /
# sqlite3_flutter_libs packages are end-of-life; do not reintroduce them.
hooks:
  user_defines:
    sqlite3:
      source: sqlite3mc

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State management + navigation
  flutter_riverpod: ^3.4.2
  go_router: ^17.5.0

  # Local database (encrypted SQLite via SQLite3MultipleCiphers, see hooks above)
  drift: ^2.34.3
  sqlite3: ^3.5.2
  path_provider: ^2.1.6
  path: ^1.9.1

  # Typed models / serialization (domain models, backup JSON format)
  json_annotation: ^4.12.0
  uuid: ^4.6.0

  # Security: DB passphrase + PIN salt live in OS-native secure storage,
  # never in the database or app preferences.
  # Pinned (not ^) at 10.3.1: 11.0.0+ raises compileSdk to Android 37,
  # which as of this writing only exists under a "37.0" minor-version
  # package name that this project's Gradle/AGP setup fails to resolve
  # ("Failed to find target with hash string 'android-37'"). Revisit once
  # that's sorted out upstream.
  flutter_secure_storage: 10.3.1

  # Dashboard charts
  fl_chart: ^1.2.0

  # Formatting / utilities
  intl: ^0.20.3
  collection: ^1.19.1
  crypto: ^3.0.7

  # Backup/restore: zip packaging, file picking, and mobile share sheet
  archive: ^4.2.0
  file_picker: ^12.0.0
  share_plus: ^13.3.0
  meta: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # Code generation: Drift tables/DAOs, json_serializable for backup DTOs.
  # (freezed is deliberately not used: as of this writing its only stable
  # major version requires Dart SDK >=3.13, newer than this project's
  # ^3.12.2 constraint, and its 3.x line resolves to a "-dev" prerelease
  # here — not something to depend on for a financial app's domain models.
  # Domain entities are hand-written immutable classes instead; see
  # lib/features/*/domain.)
  build_runner: ^2.15.1
  drift_dev: ^2.34.0
  json_serializable: ^6.14.1

  # Testing
  mocktail: ^1.0.5
  path_provider_platform_interface: ^2.1.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
